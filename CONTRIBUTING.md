# Contributing

Keep `main` buildable. Add focused tests for media parsing, provider adapters,
and selection behavior. New providers should implement `SubtitleProvider`,
declare capabilities, and document authentication and live-test requirements.